# Connection string for the dedicated read-only Postgres role.
# See README.md for the SQL that creates this role.
# Never point this at a production database.
DATABASE_URL=postgresql://myapp_readonly:<password>@localhost:5432/myapp

DEFAULT_LIMIT=100
MAX_LIMIT=1000
STATEMENT_TIMEOUT_MS=5000
